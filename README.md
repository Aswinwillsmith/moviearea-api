#moviearea-api
provide a movie api for react js app

#important commands
`npm insatll`
`npm run server`
`git init`
`git add .`
`git commit -m "your MSG"`
`git push -u origin main`
or
`git push`

#Data option
